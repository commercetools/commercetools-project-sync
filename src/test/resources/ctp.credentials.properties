# rename to ctp.credentials.properties and fill the values to run the job locally without env variables
# see SphereClientUtils for more info
source.projectKey=testSourceProjectKey
source.clientId=testSourceClientId
source.clientSecret=testSourceClientSecret

target.projectKey=testTargetProjectKey
target.clientId=testTargetClientId
target.clientSecret=testTargetClientSecret