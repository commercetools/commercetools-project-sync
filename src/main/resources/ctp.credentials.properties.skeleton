# rename to ctp.credentials.properties and fill the values to run the job locally without env variables
# see SphereClientUtils for more info
source.projectKey=<<create your project for local runs>>
source.clientId=YOUR client id without quotes
source.clientSecret=YOUR client secret without quotes

target.projectKey=<<create your project for local runs>>
target.clientId=YOUR client id without quotes
target.clientSecret=YOUR client secret without quotes