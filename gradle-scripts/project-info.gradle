group = 'com.commercetools'
version = System.getProperty('build.version') ?: versionWIP
mainClassName = 'com.commercetools.sync.Main'
description = "Dockerized CLI application which allows to automatically sync different resources between commercetools projects."

logger.quiet("\nActual Build Version: [{}]\n", version)